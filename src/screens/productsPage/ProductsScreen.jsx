import Navbar from "../../components/navigation/Navbar";

const ProductScreen = () => {
    return (
        <section>
            <Navbar />
            <h2>Products page</h2>
        </section>
    );
};

export default ProductScreen;
