import Navbar from "../../components/navigation/Navbar";

const ProfileScreen = () => {
    return (
        <section>
            <Navbar />
            <h2>Profile page</h2>
        </section>
    );
};

export default ProfileScreen;
